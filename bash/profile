# ~/.profile: executed by Bourne-compatible login shells.
# Must stay POSIX sh compatible (dash reads it on Debian/Ubuntu):
# environment variables only, nothing bash-specific.

if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

if tty -s; then
   mesg n 2>/dev/null
fi
