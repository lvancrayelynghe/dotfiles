vim/vim/autoload
vim/vim/plugged

# Per-machine additions to the mise bootstrap configuration (for example the
# Linux desktop profile). They must never carry secrets or host-specific data
# that belongs outside this public repository.
mise/config.local.toml

# Left behind by the retired dotter deployment on existing machines.
.dotter/
